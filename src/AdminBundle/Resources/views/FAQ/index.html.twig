{% extends 'AdminBundle:Home:base.html.twig' %}

{% block body_content %}

    <!-- Page Content -->

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">La Foire Aux Questions
                 <div class="action-button">
                    <a class="btn btn-primary btn-lg" role="button" href="{{path('add_to_faq') }}">Ajouter une question/réponse</a>
                </div>
            </h1>

        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel-group" id="accordion">
                {% set count = 0 %}
                 {% for questionAnswer in questionAnswers %}
                    {{ include('AdminBundle:FAQ:faq_item.html.twig', { 'questionAnswer' : questionAnswer, 'count' : count }) }}
                {% set count = count + 1 %}
                {% endfor %}
            </div>
        </div>
    </div>


    <!-- /.col-lg-12 -->

    <!-- /.row -->
{% endblock %}