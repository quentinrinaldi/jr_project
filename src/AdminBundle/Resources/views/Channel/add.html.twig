{% extends 'AdminBundle:Home:base.html.twig' %}

{% block stylesheets %}
     <link href="{{ asset('assets/admin_website/css/fileinput.min.css') }}" media="all" rel="stylesheet" type="text/css" /> 
{% endblock %}

{% block body_content %}


            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Ajout d'une chaine
                    </h1>

                </div>
                <!-- /.col-lg-12 -->
            </div>
{{ form_start(form, { 'action': path('add_channel'), 'method':'POST'}) }}
 {{ form_widget(form) }}

  <div>
    <div class="form-group">
      <button type="submit" name="save" value="Enregistrer" class="btn btn-primary">save</button>
      <a href="{{ path('admin_channels') }}" value="cancel" class="btn btn-default">cancel</a>
    </div>
  </div>
{{ form_end(form) }}

{% endblock %}

{% block scripts %}
       <script src="{{ asset('assets/admin_website/js/fileinput.min.js') }}"></script>
{% endblock %}