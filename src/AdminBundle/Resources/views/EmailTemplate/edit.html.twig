{% extends 'AdminBundle:Home:base.html.twig' %}

{% block body_content %}


  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header">Gestion des Emails
      </h1>
    </div>
    <!-- /.col-lg-12 -->
  </div>
  {{ form_start(form, { 'action': path('edit_email_template'), 'method':'POST'}) }}
  {{ form_widget(form) }}

  <div>
    <div class="form-group">
      <button type="submit" name="Confirmer" value="Confirmer" class="btn btn-primary">Confirmer</button>
      <a href="{{ path('admin_faq') }}" value="Annuler" class="btn btn-default">Annuler</a>
    </div>
  </div>
  {{ form_end(form) }}

{% endblock %}
