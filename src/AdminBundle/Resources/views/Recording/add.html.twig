{% extends 'AdminBundle:Home:base.html.twig' %}

{% block body_content %}


            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Ajout d'un enregistrement d'émission
                    </h1>

                </div>
                <!-- /.col-lg-12 -->
            </div>
{{ form_start(form, { 'action': path('add_recording', {'tvshowID' : tvshowID}), 'method':'POST'}) }}
 {{ form_widget(form) }}

  <div>
    <div class="form-group">
      <button type="submit" name="save" value="Confirmer" class="btn btn-primary">Confirmer</button>
      <a href="{{ path('admin_recordings') }}" value="Annuler" class="btn btn-default">Annuler</a>
    </div>
  </div>
{{ form_end(form) }}

{% endblock %}

{% block scripts %}
{% endblock %}