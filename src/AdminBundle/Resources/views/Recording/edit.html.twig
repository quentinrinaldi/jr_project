{% extends 'AdminBundle:Home:base.html.twig' %}

{% block body_content %}


            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Edition d'une chaine 
                      <a class="btn btn-danger btn-lg" role="button" href="recordings/add">Supprimer</a>
                    </h1>

                </div>
                <!-- /.col-lg-12 -->
            </div>
{{ form_start(form, { 'action': path('edit_recording', {'id': recording.id}), 'method':'POST'}) }}
 {{ form_widget(form) }}

  <div>
    <div class="form-group">
      <button type="submit" name="save" value="Editer" class="btn btn-primary">save</button>
      <a href="{{ path('admin_recordings') }}" value="Annuler" class="btn btn-default">cancel</a>
    </div>
  </div>
{{ form_end(form) }}
        
{% endblock %}


{% block scripts %}
  <script src="{{ asset('assets/admin/js/purify.min.js') }}"></script>
  <script>
</script>
{% endblock %}
