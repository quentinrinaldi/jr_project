{% extends 'AdminBundle:Home:base.html.twig' %}

{% block body_content %}


            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Ajout d'un fichier
                    </h1>

                </div>
                <!-- /.col-lg-12 -->
            </div>
{{ form_start(form, { 'action': path('add_upload'), 'method':'POST'}) }}
 {{ form_widget(form) }}

  <div>
    <div class="form-group">
      <button type="submit" name="save" value="Enregistrer" class="btn btn-primary">Valider</button>
      <a href="{{ path('admin_uploads') }}" value="cancel" class="btn btn-default">Annuler</a>
    </div>
  </div>
{{ form_end(form) }}

{% endblock %}

{% block scripts %}
{% endblock %}