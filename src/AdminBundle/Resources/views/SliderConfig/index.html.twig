{% extends 'AdminBundle:Home:base.html.twig' %}

{% block stylesheets %} 
<link rel="stylesheet" type="text/css" href="{{ asset('assets/admin_website/css/slider_config.css') }}"/>
{% endblock %}

{% block body_content %}

    <!-- Page Content -->

            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Configuration du slider 
                         <div class="action-button">
                            <a class="btn btn-primary btn-lg" role="button" href="{{path('add_slide') }}">Ajouter une slide</a>
                        </div>
                    </h1>

                </div>
            </div>
            {% for slide in slides %}
                {{ include('AdminBundle:SliderConfig:slide.html.twig', { 'slide' : slide }) }}
            {% endfor %}
            <!-- /.row -->
{% endblock %}