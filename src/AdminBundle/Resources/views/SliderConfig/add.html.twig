{% extends 'AdminBundle:Home:base.html.twig' %}

{% block body_content %}


            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Ajout d'une slide
                    </h1>

                </div>
                <!-- /.col-lg-12 -->
            </div>
{{ form_start(form, { 'action': path('add_slide'), 'method':'POST'}) }}
 {{ form_widget(form) }}

  <div>
    <div class="form-group">
      <button type="submit" name="save" value="Enregistrer" class="btn btn-primary">save</button>
      <a href="{{ path('slider_config') }}" value="cancel" class="btn btn-default">cancel</a>
    </div>
  </div>
{{ form_end(form) }}

{% endblock %}

{% block scripts %}
{% endblock %}